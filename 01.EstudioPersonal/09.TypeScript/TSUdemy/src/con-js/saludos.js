/**
 * 
 * @param {string} mensaje 
 * @param {number} numero
 * @return {string}
 */
export function holaMundos(mensaje, numero) {
    console.log(`Hola mundo ${mensaje} ${numero}`)

    return mensaje
}